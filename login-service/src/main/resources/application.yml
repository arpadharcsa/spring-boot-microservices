# Spring properties
spring:
  application:
     name: login-service

# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

# HTTP Server
server:
  port: 2222   # HTTP (Tomcat) port